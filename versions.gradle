ext{
    // Build and AppVersion Params
    compile_sdk_version = 29
    min_sdk_version = 21
    target_sdk_version = 29
    app_version_code = 1
    app_version_name = "1.0"

    gradle_plugin_version = "4.1.2"

    kotlin_version = "1.4.10"

    appcompat_version = "1.2.0"
    constraintlayout_version = "2.0.4"
    core_ktx_version = "1.3.2"
    material_version = "1.3.0"

    // Navigation
    navigation_fragment_version="2.3.4"
    navigation_ui_version="2.3.4"

    // Coroutines
    coroutines_version = "2.3.0"

    // Room
    room_version = "2.2.6"

    // External Libs - Retrofit (Network)
    retrofit2_version = "2.9.0"
    okhttp3_logging_version = "4.9.0"

    // External Libs - Koin (DI)
    koin_version = "2.1.5"

    // External Libs - Glide (Image Loading)
    glide_version = "4.12.0"

    // JUnit (Unit Test)
    junit_version = "4.13.2"

    // MockK (Unit Test)
    mockk_version = "1.10.6"

    // Coroutines (Unit Test)
    coroutines_test_version = "1.4.2"

    // AndroidX Core Testing (Unit Test)
    core_testing_version = "2.1.0"

    // Gson converter (Unit Test)
    gson_version = "2.8.6"

    // Instrumented Tests Dependencies
    androidtest_junit_version = "1.1.2"
    androidtest_espresso_version = "3.3.0"
}